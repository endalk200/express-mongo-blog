/**
 * Import your primary style here. The primary style is in sass format.
 * by importing it here it will be compiled and minified to css format
 */
style = require("../static/sass/style.scss")

require("bootstrap")